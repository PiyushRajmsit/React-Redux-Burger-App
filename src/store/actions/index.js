import {
    addIngredients,
    removeIngredients,
    initIngredients
} from './burgerBuilder';
import {
    purchaseBurger,
    purchaseInit,
    fetchOrder
} from './orders';

export default {
    addIngredients,
    removeIngredients,
    initIngredients,
    purchaseBurger,
    purchaseInit,
    fetchOrder
};