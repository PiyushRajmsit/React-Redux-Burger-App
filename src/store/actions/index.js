import {
    addIngredients,
    removeIngredients,
    initIngredients
} from './burgerBuilder';
import {
    purchaseBurger,
    purchaseInit,
    fetchOrder
} from './orders';

import {
    auth
} from './auth';


export default {
    addIngredients,
    removeIngredients,
    initIngredients,
    purchaseBurger,
    purchaseInit,
    fetchOrder,
    auth
};